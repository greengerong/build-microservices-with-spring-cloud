spring:
  application:
    name: service-product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: infra-config-server
management:
  security:
    enabled: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
