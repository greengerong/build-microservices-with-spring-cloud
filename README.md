# korprulu: build-microservices-with-spring-cloud

This project is used to prepare for korprulu microservice workshop.

After the cloning is complete, execute the following command in the project directory:

```
./gradlew build
```

